# 更新记录

本文件记录可验证的发布结果与产品阶段。内部需求版本不等同于已生成的公开二进制版本。

## [1.0.5] - 2026-07-26

本次更新修正 1.0.4 的远程图片加载策略，并把 WebDAV 从界面功能补全为经过真实标准服务验证的内容同步通道。

### 发布与图片热修复

- 发布固定执行保存文章、`hexo clean`、`hexo generate`、`hexo deploy`，旧设置不能再跳过清理或生成。
- 发布前校验所有 Markdown 文章头部；生成输出出现 Hexo/YAML 错误时，即使进程返回 0 也中止后续部署，不再上传缺文章的旧站点。
- 文章列表增加右键菜单，支持文章与草稿互转、在文件夹中定位，以及二次确认后移入系统回收站。
- Windows 下发布、预览、Git 同步、运行环境检查和文件定位全部静默执行，不再闪现终端窗口。
- 粘贴或拖拽图片会先写入应用本地缓存并立即插入 Markdown，远程图床上传改为后台进行，不阻塞写作。
- 上传成功后只替换 Markdown 圆括号中的图片地址，保留用户在方括号中修改的描述；文章保存成功后再清理对应缓存。
- 应用重启或内容同步重新扫描后自动恢复并续传仍被文章引用的图片缓存；地址替换、保存或会话校验任一步失败都保留缓存。
- 图片上传中禁止切换文章、切换项目、离开写作页、发布和退出，避免后台结果写入错误文章或丢失恢复依据。
- 上传未完成或失败时保留本地图片，并从前后端同时阻止发布临时图片地址。
- Windows 下 Hexo、Node.js 检测和内容同步进程无窗口运行；浏览器预览启动后提供明确的“关闭后台预览”按钮。
- 移除面向普通用户的原始任务输出和代码式日志界面，只展示可理解的处理阶段与错误提示。

### 图片加载与错误状态

- Markdown 与 HTML 远程图片改由 WebView 直接访问原始 URL，不再先由 Rust 后端完整下载、解码和重复校验，恢复连接复用与浏览器缓存能力。
- 只要响应体能作为图片显示，即使 HTTP 状态为 404 也保留服务端实际返回的图片内容。
- 空响应、网络失败或不可渲染内容在正文中显示保留尺寸的错误框；文章列表显示封面错误图标，不再回退为默认封面或沿用旧图。
- 封面一旦确认失败，错误提示会按原始图片地址保留；切换文章不会重新闪现失败图片，后台复检成功后才恢复显示。
- 隐藏即时预览后，Markdown 编辑器会占满文章列表之外的全部剩余空间，不再保留预览区域空白。
- 本地图片继续通过项目会话约束的资源协议读取，路径边界和目录逃逸防护保持不变。

### WebDAV 内容同步

- 新增标准 WebDAV Basic Auth 同步，支持 `PROPFIND`、`MKCOL`、`PUT`、`GET` 和 `DELETE`，生产环境只接受 HTTPS 地址。
- 服务器地址、远端目录、用户名和密码在启用后始终可见可编辑；用户名可从系统凭据库回显，密码永不返回前端，留空表示复用同服务器的已保存密码。
- “保存并测试连接”会先使用候选凭据完成目录访问、随机可逆读写删除探针和远端清单校验，全部成功后才覆盖系统凭据；失败时保留旧凭据与当前输入。
- 修改服务器或远端目录后清除旧预检并标记为尚未应用；只有重新测试通过后才能应用，后端应用时还会再次访问远端验证。
- 更换远端连接后不自动上传或下载，而是重新要求选择首次同步方向；只修复凭据时保留已有同步历史。
- WebDAV 使用按 SHA-256 命名的不可变对象，最后通过 ETag 条件更新 `.hexo-lite-sync.json`；远端已前进时停止写入，不静默覆盖。
- 认证、网络、权限、只读目录、清单损坏和探针清理失败使用独立错误码；凭据、Authorization 与探针内容不会进入日志、配置或同步清单。

### 构建与验证

- 增加 WsgiDAV 4.3.5 Basic Auth 真实服务测试，覆盖错误密码不覆盖旧凭据、上传下载、认证恢复、连接变更和 ETag 过期保护。
- 增加真实 Tauri IPC 测试，覆盖命令路由、camelCase 参数反序列化、状态注入、系统凭据库和真实 HTTP 请求的完整链路。
- 浏览器测试只承担表单可编辑性、失败恢复、密码清空和未重测禁止应用等交互验收，不再把 browser mock 当作 WebDAV 兼容性依据。
- 补齐 macOS Intel/Apple Silicon 通用应用与 DMG 构建、架构校验、诊断日志和发布产物上传流程。

详细范围、版本要求和验收标准见 [1.0.5 发布说明](.github/release-notes/v1.0.5.md)。

## [1.0.4] - 2026-07-23

本次更新集中改善即时预览、图片真实性、图床配置、设置页层级与桌面窗口体验。

### 即时预览与图片

- 移除软件内嵌的 Hexo 主题 iframe，保留“浏览器预览”在系统浏览器中打开真实博客页面。
- 即时预览支持经过清理的常用原生 HTML；脚本、事件属性、iframe、表单、危险 URL 和越界样式继续被拦截。
- 远程图片在 1.0.4 中由 Rust 后端重新请求并校验公网地址、重定向、MIME、真实图片类型和大小，不复用上一次成功缓存。
- 图片被删除、返回错误状态、类型不符、超过限制或无法验证时，统一显示“图片不可用”。

### 图床、设置与窗口

- 本地图床支持配置 `source/` 下的图片目录和 Markdown 前缀；导入、粘贴、拖放、列表和引用共用同一配置。
- CloudFlare-ImgBed 支持创建受限权限 Token、测试连接和删除本地 Token；管理员密码不落盘，Token 只存入系统凭据库。
- 设置页重组为五个分类，并修复长内容分类切换时滚动条造成的横向抖动。
- 增加不超过 200ms 的轻量页面过渡，并适配 `prefers-reduced-motion`。
- 统一新版灰银应用图标在窗口、任务栏、标题栏、侧栏、关于页和安装包中的显示。
- 修复右上角关闭无法退出、关闭确认重复触发，以及确认框等待期间自动保存破坏“不保存退出”语义的问题。

### 安全、兼容与发布

- 保持配置 `schemaVersion: 3`，自动兼容 1.0.3 配置并补齐新增图床字段。
- 本地图片路径继续拒绝绝对路径、`..` 与符号链接逃逸；Token、管理员密码和敏感请求头不会进入配置或任务日志。
- 将 `linkify-it` 固定至 5.0.2，修复邮件链接匹配可能触发的高复杂度拒绝服务问题。
- 重写中英文项目首页、真实界面截图、下载说明和 Windows 安装版、MSI、便携版校验流程。

详细范围、版本要求和验收标准见 [1.0.4 发布说明](.github/release-notes/v1.0.4.md)。

## [1.0.3] - 2026-07-18

首个按完整桌面产品标准整理的正式版本。

- 使用紧凑项目切换顶栏和四工作区导航，发布保留在编辑器内并支持 `Ctrl + Shift + P`。
- 修复长文章、文章列表和预览的独立滚动以及深色模式光标。
- 提供安全 Markdown 与隔离 Hexo 主题双模式预览，并管理 Hexo Server 完整生命周期。
- 图床升级为目录资源浏览器，支持正确文件类型图标、统一上下文菜单和图片灯箱。
- 配置升级至 V3；增加项目会话、保存 revision、防路径越权、凭据库和脱敏日志轮转。
- 删除发布页、任意终端、任意 Shell、任意路径 IPC、云同步和自动弹出的任务日志抽屉。
- 增加 WebView2 启动前检测、NSIS/MSI 安装包和免安装 ZIP 发布流程。

## [1.0.2] - 产品整改阶段

- 确立 Quiet Pro 桌面视觉、Tauri 独立窗口和稳定工作区信息架构。
- 规划并实施安全 IPC、项目会话、保存状态机、凭据保护和 Rust 模块拆分。
- 该阶段直接演进为 1.0.3，未保留可与需求逐字对应的独立源码快照或正式安装包。

## [1.0.1] - 2026-06-26

- 形成早期 Hexo 桌面编辑、Markdown 预览、图床、生成、部署和日志能力。
- 存在可归档的 Windows NSIS/MSI 二进制，但无法证明与当前 Git 历史中的某一提交完全一致，因此仅作为历史存档。

## [1.0.0] - 产品基线阶段

- 定义打开 Hexo 项目、扫描文章、Markdown 编辑、图片插入、预览和发布的基础闭环。
- 未发现能够可靠验证的 1.0.0 源码快照或同版本二进制，不创建虚假的 `v1.0.0` Release。

## 历史开发构建

- `0.2.0`：现存 Windows NSIS/MSI 二进制，精确变更记录和源码映射不可验证。
- `0.1.0`：现存最早 Windows NSIS/MSI 二进制，精确变更记录和源码映射不可验证。
