# 更新记录

本文件记录可验证的发布结果与产品阶段。内部需求版本不等同于已生成的公开二进制版本。

## [1.0.6.5] - 2026-09-18

- 预览切换为分块渲染管线：`previewBlocks.ts` 按顶层块拆分并复用上帧指纹，`renderScheduler.ts` 自适应合帧与跳帧，`previewSurface.ts` 只替换变化块的 DOM；长文打开与连续输入的主线程峰值明显下降。
- 滚动同步改为编辑器 → 预览单向跟随：预览区自身滚动不再回写编辑器，跨段反复滚动不再累积误差；首尾仍按滚动范围边界直接对齐。
- 编辑器会话通道化（`EditorSessionStore` 按 content/selection/saving/session 分组通知），`EditorPage` 状态拆为原子字段，`MarkdownEditor` 扩展切换为 Compartment，单一状态变化不再触发整页重建。
- 设置全部改为改完即生效：350 ms 防抖落盘，无保存/放弃按钮；校验失败自动回滚、重选分区并聚焦出错字段；移除 `features/settings/controller.ts`；"图片插入方式"假设置删除。
- 文件同步设置改版：首次配置保留三步向导；启用态收敛为状态卡 + 一行待上传摘要（细节默认折叠）；连接块提供 GitHub/WebDAV 标签，点另一通道进入其设置并通过同样的预检后"切换到 X 并合并"，无需先关闭同步，旧通道数据不受影响。
- 深浅双主题重写：浅色暖纸 + 墨绿、深色冷灰 + 钢蓝；暗色令牌单一来源，"跟随系统"改由 JS 解析 `matchMedia` 实时切换；全部硬编码颜色收敛为 design token，正文与三级文字对比度满足 WCAG AA。
- 全站文案分批改写为更直白的中文主文案并同步英文，i18n 词条表随之收口（删除废弃保存语义与假设置词条）；修复弹窗焦点、危险确认、通知队列等五处交互硬伤。
- 新增 DEV 专用性能埋点 `window.__previewStats` / `window.__editorStats`，生产构建不含统计开销。
- 对外 `1.0.6.5`，内部 `1.0.6+5`，MSI `1.0.6.5`，macOS bundle `1.0.605`。

验证与发布状态见 [验证记录](docs/validation-1.0.6.5.md)，需求与实现见 [版本归档](docs/archive/1.0.6.5.md)。

## [1.0.6.4] - 2026-09-18

- 首屏只等待配置，更新状态与最近项目恢复改到首帧后执行；移除启动时全部页面预加载，项目验证、文章扫描和资源恢复转入原生阻塞任务线程。
- 即时预览支持文章内 `<style>`、class 和 CSS 自定义变量；样式选择器限定在预览容器，继续过滤脚本、外部 CSS、危险 URL、事件与越界定位。
- Markdown 渲染与本地图片源收集合并为一次解析；长文输入采用可取消的帧后任务，减少打开文章和连续输入时的主线程峰值。
- 双向滚动同步补齐首尾滚动范围，修复反复跨段滚动后的位置漂移；预览主控时的锚点重测不再读取编辑器尚未完成跳转的旧边界，HTML 或图片布局变化也不会把预览推回旧位置。
- 文件同步页改为清晰的三步规划和 GitHub/WebDAV 双选卡；只显示所选通道的配置。项目扫描延迟到进入同步分类后执行，状态、凭据和关闭同步中的阻塞工作转入原生 worker，检查期间持续显示进度并可停止。
- 新增模块职责与调用链文档，版本检查会要求当前版本的模块基线、修改归档、验证记录和发布说明同时存在。
- 将 Svelte 传递依赖 `devalue` 固定到 5.9.2，修复发布前依赖审计发现的拒绝服务安全公告。
- 对外 `1.0.6.4`，内部 `1.0.6+4`，MSI `1.0.6.4`，macOS bundle `1.0.604`。

验证与发布状态见 [验证记录](docs/validation-1.0.6.4.md)，需求与实现见 [版本归档](docs/archive/1.0.6.4.md)。

## [1.0.6.3] - 2026-09-16

- 补齐 Markdown 内 HTML 的对齐、垂直对齐、背景色和旧式字体属性，保留内联样式优先级与声明图片尺寸。
- HTML 表格和折叠内容直接记录源码锚点，修复额外标记影响结构/滚动定位；代码块和现有图片解析保持兼容。
- 统一 Windows/macOS 表单外观；设置数字字段实时写入草稿，快捷键保存无需失焦；分类切换重置滚动，吸顶随实际页头高度调整。
- 修复发布保存期间新图片上传可能绕过发布前检查、WebKit 源码同步亚像素偏差，以及页面切换时离场页面偶发拦截点击。
- 发布快捷键、图片上传和自动更新回归改用显式状态同步；CI 浏览器矩阵使用单 worker，消除托管环境并发冷启动导致的随机空白页超时。
- Chromium/WebKit 运行相同交互回归，macOS 打包前增加原平台完整 WebKit 检查。
- 对外 `1.0.6.3`，内部 `1.0.6+3`，MSI `1.0.6.3`，macOS bundle `1.0.603`。

验证与发布状态见 [验证记录](docs/validation-1.0.6.3.md)，需求与实现见 [版本归档](docs/archive/1.0.6.3.md)。

## [1.0.6.2] - 2026-09-14

- 完整站点源码增量同步与首次安全并集；手动同步自动合并不同文件，同文件修改或删除冲突逐项选择，整项目覆盖移入高级操作。
- Git/WebDAV 缓存只更新变化文件，WebDAV 按实际唯一对象显示进度；修复换行和 Git 属性改写同步字节、忽略文件无法同步、扫描异常被误报完成等问题。
- 设置重排为六组，缩短说明、折叠连接及危险操作，新增范围和变化统计、手动发布入口与窄窗口布局。
- 更新默认只静默检查、手动下载；显示真实百分比、速度、预计时间和验签阶段，默认展示 5 条更新摘要，下载完成后点击安装。
- Windows 后续升级采用 quiet 模式，页面和对话框过渡支持减少动态效果。云端双平台构建完成后统一核对源码、SHA256、包大小与 Minisign 签名，再公开 Release。
- 对外版本 `1.0.6.2`，内部 `1.0.6+2`，MSI `1.0.6.2`，macOS bundle `1.0.602`。

验证结果与限制见 [1.0.6.2 验证记录](docs/validation-1.0.6.2.md)，需求与实现归档见 [版本归档](docs/archive/1.0.6.2.md)。

## [1.0.6.1] - 2026-09-11

- GitHub / WebDAV 网络同步移至后台线程，增加阶段、文件数量、计时、停止与重试；WebDAV 复用经过 hash 校验的缓存与已有对象，减少重复传输。打开项目先加载本地，再后台检查云端。
- 编辑器与即时预览使用源码行锚点和段落插值双向同步，图片加载和窗口变化后重新定位；真实主题预览仍由 Hexo 单独处理。
- 插件迁至独立导航页，新增 Ctrl+5；保留原 Ctrl+1 至 Ctrl+4。
- 新增“全部文件”（Ctrl+6）：懒加载目录树、相对路径打开、文本高亮、显式保存、磁盘内容比较、冲突处理和离开保护；二进制与超过 2 MB 的文件只读，可在系统文件管理器中定位。
- 公开版本和界面显示为 1.0.6.1，Tauri / Cargo 及更新清单使用 1.0.6+1；Windows MSI 版本为 1.0.6.1。
- 2026-09-12 补充：插件配置关闭确认与加载重试、窄窗口工具栏、预览旧坐标失效、同步取消会话校验；双平台发布核验同一源码提交和 Windows 安装包 hash。早期 1.0.6.1 用户需重新下载安装包，同版本不触发自动更新。

验证范围及发布状态见 [1.0.6.1 验证记录](docs/validation-1.0.6.1.md)。

## [1.0.6-r1] - 2026-09-10

应用版本维持 **1.0.6**，已发布独立标签 [`v1.0.6-r1`](https://github.com/Bai-YB/hexo-lite-editor/releases/tag/v1.0.6-r1)，保留原 `v1.0.6` 标签与发布。原 1.0.6 用户需手动下载并重新安装；同版本不会触发自动更新。跨平台 CI、Windows 安装器验证及下载资产校验见 [修复验证记录](docs/fix-validation-1.0.6-r1.md)。

- 文章与同步：保护等待期间的输入和文档身份，修复连续保存取消自动同步、旧清单误删范围外文件、上传字节/hash 不一致、跨项目重扫与旧同步设置回写；外部更改由用户比较并决定保留本地、采用远端或另存恢复稿。
- 编辑器：修复图片导入插错文章、上传后撤销失效链接、切文确认期间自动保存、保存基线、文件名跟随、本地日期、错误重试目标及文章/草稿资源目录互转；真实预览统一先保存当前内容。
- 设置：保存回包保留后续输入，主题/语言预览可恢复，Token 操作只保存必要连接字段；数字错误可见并聚焦，GitHub 与 WebDAV 共用冲突处理和重新检查入口。
- 交互与资源：模态隔离快捷键、异步提交防重；取消导入不清空列表，部分失败明确反馈，查询乱序和末页删空可恢复；插件默认值真实保存、Worker 故障后重建、卸载默认保留设置；更新错误可恢复和重试。
- 审计中的扩展方案与本轮已实施修改分别记录；完整图片引用分析、备份管理器、流式下载任务等不作为本构建已有功能宣传。

## [1.0.6] - 2026-09-09

本次更新完成中英文界面、Cloudflare-ImgBed 目录适配、插件 API、真实主题预览和签名更新流程，并补强跨设备项目同步。

### 项目同步

- GitHub 与 WebDAV 同步范围升级为 Hexo 项目源文件，覆盖文章、草稿、`source/`、主题、脚手架、依赖声明和 Hexo/主题配置。
- 默认排除 `.git`、`.deploy_git`、`public`、`node_modules`、缓存、临时文件、`.env` 与常见凭据；旧版同步清单保持兼容。
- 云端前进时不再卡在提示上：可以使用云端最新项目覆盖本机，或以云端最新提交为父提交、用本机项目内容覆盖云端，避免强制改写 Git 历史。
- 云端覆盖本机前创建本地备份；公开或可见性未知的旧 GitHub 同步配置，扩大同步范围前必须再次确认。

### 图床与更新

- Cloudflare-ImgBed 图床页按当前路径上传：`/` 对应根目录，`/blog` 对应根目录下的 `blog`，文章编辑器继续上传到 `blog/<文章目录>`。
- 启动后每天最多检查一次更新；发现更高版本后自动后台下载，并由 Tauri 校验更新包签名，验证通过后再提示用户重启安装。
- 自动下载失败会显示错误；更新检查失败不会写入每日成功时间，后续启动仍可重试。
- Windows 与 macOS 工作流共同发布安装包、更新包、签名、校验和与跨平台 `latest.json`。

### 其他能力

- 新增中英文界面与系统语言匹配、Plugin API v0.1 与权限隔离、Cloudflare-ImgBed 深度资源操作，以及更贴近真实 Hexo 主题的浏览器预览。
- 文章改名会同步本地文件名、标题和图床目录记录；上游没有安全原子重命名接口时，不伪造远端文件夹迁移。
- DOMPurify 升级至 3.4.15，修复上游公布的 `IN_PLACE` hook 移除场景 XSS 问题。

详细内容见 [1.0.6 发布说明](.github/release-notes/v1.0.6.md)。

## [1.0.5] - 2026-07-26

本次更新修正 1.0.4 的远程图片加载策略，并把 WebDAV 从界面功能补全为经过真实标准服务验证的内容同步通道。

### 发布与图片热修复

- 发布固定执行保存文章、`hexo clean`、`hexo generate`、`hexo deploy`，旧设置不能再跳过清理或生成。
- 发布前校验所有 Markdown 文章头部；生成输出出现 Hexo/YAML 错误时，即使进程返回 0 也中止后续部署，不再上传缺文章的旧站点。
- 文章列表增加右键菜单，支持文章与草稿互转、在文件夹中定位，以及二次确认后移入系统回收站。
- Windows 下发布、预览、Git 同步、运行环境检查和文件定位全部静默执行，不再闪现终端窗口。
- 粘贴或拖拽图片会先写入应用本地缓存并立即插入 Markdown，远程图床上传改为后台进行，不阻塞写作。
- 上传成功后只替换 Markdown 圆括号中的图片地址，保留用户在方括号中修改的描述；文章保存成功后再清理对应缓存。
- 应用重启或内容同步重新扫描后自动恢复并续传仍被文章引用的图片缓存；地址替换、保存或会话校验任一步失败都保留缓存。
- 图片上传中禁止切换文章、切换项目、离开写作页、发布和退出，避免后台结果写入错误文章或丢失恢复依据。
- 上传未完成或失败时保留本地图片，并从前后端同时阻止发布临时图片地址。
- Windows 下 Hexo、Node.js 检测和内容同步进程无窗口运行；浏览器预览启动后提供明确的“关闭后台预览”按钮。
- 移除面向普通用户的原始任务输出和代码式日志界面，只展示可理解的处理阶段与错误提示。

### 图片加载与错误状态

- Markdown 与 HTML 远程图片改由 WebView 直接访问原始 URL，不再先由 Rust 后端完整下载、解码和重复校验，恢复连接复用与浏览器缓存能力。
- 只要响应体能作为图片显示，即使 HTTP 状态为 404 也保留服务端实际返回的图片内容。
- 空响应、网络失败或不可渲染内容在正文中显示保留尺寸的错误框；文章列表显示封面错误图标，不再回退为默认封面或沿用旧图。
- 封面一旦确认失败，错误提示会按原始图片地址保留；切换文章不会重新闪现失败图片，后台复检成功后才恢复显示。
- 隐藏即时预览后，Markdown 编辑器会占满文章列表之外的全部剩余空间，不再保留预览区域空白。
- 本地图片继续通过项目会话约束的资源协议读取，路径边界和目录逃逸防护保持不变。

### WebDAV 内容同步

- 新增标准 WebDAV Basic Auth 同步，支持 `PROPFIND`、`MKCOL`、`PUT`、`GET` 和 `DELETE`，生产环境只接受 HTTPS 地址。
- 服务器地址、远端目录、用户名和密码在启用后始终可见可编辑；用户名可从系统凭据库回显，密码永不返回前端，留空表示复用同服务器的已保存密码。
- “保存并测试连接”会先使用候选凭据完成目录访问、随机可逆读写删除探针和远端清单校验，全部成功后才覆盖系统凭据；失败时保留旧凭据与当前输入。
- 修改服务器或远端目录后清除旧预检并标记为尚未应用；只有重新测试通过后才能应用，后端应用时还会再次访问远端验证。
- 更换远端连接后不自动上传或下载，而是重新要求选择首次同步方向；只修复凭据时保留已有同步历史。
- WebDAV 使用按 SHA-256 命名的不可变对象，最后通过 ETag 条件更新 `.hexo-lite-sync.json`；远端已前进时停止写入，不静默覆盖。
- 认证、网络、权限、只读目录、清单损坏和探针清理失败使用独立错误码；凭据、Authorization 与探针内容不会进入日志、配置或同步清单。

### 构建与验证

- 增加 WsgiDAV 4.3.5 Basic Auth 真实服务测试，覆盖错误密码不覆盖旧凭据、上传下载、认证恢复、连接变更和 ETag 过期保护。
- 增加真实 Tauri IPC 测试，覆盖命令路由、camelCase 参数反序列化、状态注入、系统凭据库和真实 HTTP 请求的完整链路。
- 浏览器测试只承担表单可编辑性、失败恢复、密码清空和未重测禁止应用等交互验收，不再把 browser mock 当作 WebDAV 兼容性依据。
- 补齐 macOS Intel/Apple Silicon 通用应用与 DMG 构建、架构校验、诊断日志和发布产物上传流程。

详细范围、版本要求和验收标准见 [1.0.5 发布说明](.github/release-notes/v1.0.5.md)。

## [1.0.4] - 2026-07-23

本次更新集中改善即时预览、图片真实性、图床配置、设置页层级与桌面窗口体验。

### 即时预览与图片

- 移除软件内嵌的 Hexo 主题 iframe，保留“浏览器预览”在系统浏览器中打开真实博客页面。
- 即时预览支持经过清理的常用原生 HTML；脚本、事件属性、iframe、表单、危险 URL 和越界样式继续被拦截。
- 远程图片在 1.0.4 中由 Rust 后端重新请求并校验公网地址、重定向、MIME、真实图片类型和大小，不复用上一次成功缓存。
- 图片被删除、返回错误状态、类型不符、超过限制或无法验证时，统一显示“图片不可用”。

### 图床、设置与窗口

- 本地图床支持配置 `source/` 下的图片目录和 Markdown 前缀；导入、粘贴、拖放、列表和引用共用同一配置。
- CloudFlare-ImgBed 支持创建受限权限 Token、测试连接和删除本地 Token；管理员密码不落盘，Token 只存入系统凭据库。
- 设置页重组为五个分类，并修复长内容分类切换时滚动条造成的横向抖动。
- 增加不超过 200ms 的轻量页面过渡，并适配 `prefers-reduced-motion`。
- 统一新版灰银应用图标在窗口、任务栏、标题栏、侧栏、关于页和安装包中的显示。
- 修复右上角关闭无法退出、关闭确认重复触发，以及确认框等待期间自动保存破坏“不保存退出”语义的问题。

### 安全、兼容与发布

- 保持配置 `schemaVersion: 3`，自动兼容 1.0.3 配置并补齐新增图床字段。
- 本地图片路径继续拒绝绝对路径、`..` 与符号链接逃逸；Token、管理员密码和敏感请求头不会进入配置或任务日志。
- 将 `linkify-it` 固定至 5.0.2，修复邮件链接匹配可能触发的高复杂度拒绝服务问题。
- 重写中英文项目首页、真实界面截图、下载说明和 Windows 安装版、MSI、便携版校验流程。

详细范围、版本要求和验收标准见 [1.0.4 发布说明](.github/release-notes/v1.0.4.md)。

## [1.0.3] - 2026-07-18

首个按完整桌面产品标准整理的正式版本。

- 使用紧凑项目切换顶栏和四工作区导航，发布保留在编辑器内并支持 `Ctrl + Shift + P`。
- 修复长文章、文章列表和预览的独立滚动以及深色模式光标。
- 提供安全 Markdown 与隔离 Hexo 主题双模式预览，并管理 Hexo Server 完整生命周期。
- 图床升级为目录资源浏览器，支持正确文件类型图标、统一上下文菜单和图片灯箱。
- 配置升级至 V3；增加项目会话、保存 revision、防路径越权、凭据库和脱敏日志轮转。
- 删除发布页、任意终端、任意 Shell、任意路径 IPC、云同步和自动弹出的任务日志抽屉。
- 增加 WebView2 启动前检测、NSIS/MSI 安装包和免安装 ZIP 发布流程。

## [1.0.2] - 产品整改阶段

- 确立 Quiet Pro 桌面视觉、Tauri 独立窗口和稳定工作区信息架构。
- 规划并实施安全 IPC、项目会话、保存状态机、凭据保护和 Rust 模块拆分。
- 该阶段直接演进为 1.0.3，未保留可与需求逐字对应的独立源码快照或正式安装包。

## [1.0.1] - 2026-06-26

- 形成早期 Hexo 桌面编辑、Markdown 预览、图床、生成、部署和日志能力。
- 存在可归档的 Windows NSIS/MSI 二进制，但无法证明与当前 Git 历史中的某一提交完全一致，因此仅作为历史存档。

## [1.0.0] - 产品基线阶段

- 定义打开 Hexo 项目、扫描文章、Markdown 编辑、图片插入、预览和发布的基础闭环。
- 未发现能够可靠验证的 1.0.0 源码快照或同版本二进制，不创建虚假的 `v1.0.0` Release。

## 历史开发构建

- `0.2.0`：现存 Windows NSIS/MSI 二进制，精确变更记录和源码映射不可验证。
- `0.1.0`：现存最早 Windows NSIS/MSI 二进制，精确变更记录和源码映射不可验证。
